spring:
    datasource:
        url: jdbc:mysql://localhost:3306/clonemovie?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC&allowPublicKeyRetrieval=true
        username: root
        password: min_05078728
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: create-drop

        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL8Dialect
                show_sql: true
                format_sql: true
#    database-platform:

logging:
    level:
        org.springframework.security: DEBUG
